// DELETE: Depreciated — Plans utility tied to Stripe-shaped plans. Use `features/billing` equivalents.
export const findPlanByKey = (key: string) => {
	// Deprecated helper — return null to avoid runtime/indexing until migrated.
	return null as any;
};

