/**
 * TemplateCreator Component Types
 * 
 * Re-exports core slide types for convenience.
 * Component-specific types can be added here if needed.
 */

export type {
  TextElement,
  BackgroundConfig,
  SlideDesign,
} from '@/types/slide';
